services:
  nginx_test_env:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nginx_sec_remedy_test
    ports:
      - "8080:80"
      - "8443:443"
      - "2222:22"
    volumes:
      - ./workspace:/etc/nginx
    restart: "no"
