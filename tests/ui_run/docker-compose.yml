services:
  nginx_test_env:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nginx_sec_remedy
    ports:
      - "${NGINX_PORT}:22"
    volumes:
      - ./workspace:/etc/nginx
    restart: "no"
