server {
    listen 80;
    server_name static.local;
    
    # Fixed: X-Content-Type-Options added
    add_header X-Content-Type-Options "nosniff" always;
    
    location /assets {
        alias /var/www/assets;
        autoindex on;
    }
}
