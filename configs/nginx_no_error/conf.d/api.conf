server {
    listen 8080;
    server_name api.local;
    
    # Fixed: server_tokens is turned off
    server_tokens off;
    
    location / {
        return 200 '{"status":"ok"}';
        add_header Content-Type application/json;
    }
}
