server {
    listen 8080;
    server_name api.local;
    
    # Intentional mistake: server_tokens is explicitly on
    server_tokens on;
    
    location / {
        return 200 '{"status":"ok"}';
        add_header Content-Type application/json;
    }
}
